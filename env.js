/* 
To run on ios simulator: 
- set baseUrl: 'http://localhost:3000'

To run on iphone: 
- make sure your iphone is on the same wifi network as your machine where the server is running locally
- set baseUrl: 'http://YOUR_WIFI_NETWORK_IP_ADDRESS:3000'
*/

const vars = {
    baseUrl: 'http://192.168.1.13:3000'
};

export default vars;